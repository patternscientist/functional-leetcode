# 0033. Search in Rotated Sorted Array

Status: accepted Haskell initial import.

Judge shape:

- examples and hard cases from `inputs/Lib.hs`;
- generated rotations of sorted unique arrays checked against linear search.

Acceptance evidence: `python judge/judge.py test 0033 haskell` passes 2593 /
2593 fixed/generated cases.
