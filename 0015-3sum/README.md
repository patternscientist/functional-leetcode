# 0015. 3Sum

Status: accepted Haskell initial import.

Judge shape:

- examples and hard duplicate cases from `inputs/Lib.hs`;
- generated small arrays checked against an O(n^3) canonical triplet oracle.

Acceptance evidence: `python judge/judge.py test 0015 haskell` passes 71 / 71
fixed/generated cases.
