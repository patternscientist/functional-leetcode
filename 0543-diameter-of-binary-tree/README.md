# 0543. Diameter of Binary Tree

Status: accepted Haskell initial import.

Judge shape:

- fixed tree tests from `inputs/Lib.hs`;
- generated trees checked against a slow oracle that computes every through-node
  diameter.

Acceptance evidence: `python judge/judge.py test 0543 haskell` passes 40 / 40
fixed/generated cases.
