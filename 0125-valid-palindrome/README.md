# 0125. Valid Palindrome

Status: accepted Haskell initial import.

Judge shape:

- examples and hard normalization cases from `inputs/Lib.hs`;
- generated strings checked against a normalize-then-reverse specification.

Acceptance evidence: `python judge/judge.py test 0125 haskell` passes 92 / 92
fixed/generated cases.
