# 0994. Rotting Oranges

Status: accepted Haskell initial import.

Judge shape:

- examples and hard case from `inputs/Lib.hs`;
- generated grids checked against an independent minute-by-minute simulator.

Acceptance evidence: `python judge/judge.py test 0994 haskell` passes 154 / 154
fixed/generated cases.
